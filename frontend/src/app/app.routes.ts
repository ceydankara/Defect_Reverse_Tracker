import { Routes } from '@angular/router';
import { DefectTrackerComponent } from './features/defect-tracker/defect-tracker';

export const routes: Routes = [
  { path: '', component: DefectTrackerComponent }
];
